import userData from 'data/userData';
import appData from 'data/appData';

const {
  updateUrl,
} = appData.actions;

const {
  updateCredentials,
} = userData.actions;

export {
  updateUrl,
  updateCredentials,
}
