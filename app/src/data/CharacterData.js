import { BLINKS, NOOK, PEARL, DOUGLAS } from 'data/CharacterSchema';
// default state
const state_default = {
  characters: [
    BLINKS, NOOK, PEARL, DOUGLAS
  ],
};

const CharacterData = state_default;

// export
export default CharacterData;

export {
  state_default,
};
